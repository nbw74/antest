---
argument_specs:
  main:
    short_description: The main entry point for the build role.
    options:
      build_force_dnf:
        type: bool
        default: false
      build_skip_ssh_host_keys:
        type: bool
        default: false
      build_skip_ssh_service:
        type: bool
        default: false
      build_ssh_port:
        type: int
        default: 2222
...
